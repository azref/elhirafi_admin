#file: noinspection YAMLSchemaValidation
name: elhirafi_admin
description: Admin Dashboard for Elhirafi App.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # الحزم الأساسية للوحة التحكم
  supabase_flutter: ^2.5.0
  provider: ^6.1.2
  flutter_dotenv: ^5.1.0
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # تعريف مسار ملف المفاتيح السرية
  assets:
